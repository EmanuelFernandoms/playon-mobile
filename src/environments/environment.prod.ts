/**
 * Environment de produção
 * 
 * IMPORTANTE: Antes de fazer o build para produção, atualize a apiBaseUrl
 * com a URL pública do seu servidor de API.
 * 
 * Exemplo:
 * apiBaseUrl: 'https://meusite.com/api'
 * ou
 * apiBaseUrl: 'https://api.meusite.com'
 */
export const environment = {
  production: true,
  apiBaseUrl: 'http://playon.app.br/playon/api' // ⚠️ SUBSTITUA pela URL pública do seu site
};
